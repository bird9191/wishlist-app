# 🎁 Wishlist App - Социальный вишлист

Приложение для создания списков желаний с возможностью резервирования подарков и коллективных сборов.

## 🚀 Технологии

- **Frontend**: Next.js 14, React 18, TypeScript, Tailwind CSS
- **Backend**: FastAPI, Python 3.9+
- **Database**: PostgreSQL 15
- **Real-time**: WebSockets
- **Auth**: JWT (email + password), OAuth готов к подключению
- **Деплой**: Docker Compose

## ✨ Основные фичи

### 1. Секретное резервирование 🔒
- Друзья резервируют подарки, но **владелец вишлиста не видит кто** что зарезервировал
- Сюрприз сохранён! Владелец видит только факт резервирования

### 2. Коллективные сборы 💰
- Несколько друзей могут скинуться на дорогой подарок
- Прогресс-бар показывает сколько собрано
- Участники видят друг друга и суммы вкладов
- **Владелец не видит имена участников и суммы** - только общий прогресс

### 3. Real-time обновления ⚡
- WebSocket подключение
- Резервирования и вклады отображаются мгновенно без перезагрузки
- Автопереподключение при разрыве связи

### 4. Автозаполнение по ссылке ✨
- Вставьте URL товара из Ozon, Wildberries, Яндекс.Маркет, Amazon
- Приложение автоматически подтянет название, фото и цену

### 5. Публичные ссылки без регистрации
- Поделитесь вишлистом через уникальную ссылку
- Друзья могут резервировать и скидываться **без регистрации**
- Только создание вишлиста требует аккаунт

## 🎯 Продуктовые решения

### Edge cases:

1. **Товар удалили, а на него скидывались?**
   - Все резервации и вклады удаляются автоматически (cascade delete)
   - Участникам показывается уведомление

2. **Сумма не набралась к событию?**
   - Владелец видит прогресс и может продлить сбор или заменить товар
   - Участники могут отменить вклады (функционал для v2)

3. **Кто-то зарезервировал но не купил?**
   - Резервация не блокирует навсегда - можно отменить
   - Владелец видит дату резервации

4. **Минимальный вклад?**
   - Минимум: 1 рубль (валидация на backend)
   - Максимум: не больше оставшейся суммы

5. **Пустой вишлист?**
   - Красивый empty state с подсказками
   - Кнопка "Добавить первый товар"

### UX решения:

- **Градиентные прогресс-бары** (green→teal) для визуализации сборов
- **Модальные окна** для резервирования/вкладов с валидацией
- **Тосты** для всех действий (успех/ошибка)
- **Адаптивный дизайн** для мобильных
- **Индикатор WebSocket** - показывает статус подключения
- **Приоритеты** подарков (высокий/средний/низкий)

## 📦 Быстрый старт

### Через Docker (рекомендуется):

```bash
# 1. Клонировать репозиторий
git clone <repo-url>
cd project1

# 2. Создать .env файл
cp backend/.env.example backend/.env

# 3. Запустить
docker compose up -d

# 4. Открыть
http://localhost:3000
```

### Локально:

```bash
# Backend
cd backend
pip install -r requirements.txt
createdb wishlist_db
uvicorn app.main:app --reload --port 8000

# Frontend
cd frontend
npm install
npm run dev
```

## 🏗 Архитектура

### Backend API:
```
POST   /api/auth/register          - Регистрация
POST   /api/auth/login             - Вход
GET    /api/wishlists              - Мои вишлисты
POST   /api/wishlists              - Создать вишлист
GET    /api/wishlists/public/{slug} - Публичный вишлист (без auth)
POST   /api/items/{id}/reserve     - Зарезервировать (без auth)
POST   /api/items/{id}/contribute  - Скинуться (без auth)
POST   /api/url/parse              - Парсинг URL
WS     /api/items/ws/{id}          - WebSocket для real-time
```

### Database Schema:
```
User (id, email, username, password)
  ↓
Wishlist (id, title, slug, event_date, is_public)
  ↓
WishlistItem (id, title, price, is_pooling, is_reserved)
  ↓
  ├─ Reservation (id, reserver_name, reserver_email) [СКРЫТО от owner]
  └─ Contribution (id, contributor_name, amount) [СКРЫТО от owner]
```

### Секретность реализована через:
- Разные Pydantic схемы: `WishlistOwner` vs `WishlistGuest`
- Owner схема не включает `reserver_name`, `contributor_name`
- Guest схема показывает всё

## 📱 Демо сценарий

1. **Владелец (Маша):**
   - Регистрируется → Создаёт вишлист "День рождения"
   - Добавляет товар, вставляет URL Ozon → автозаполнение ✨
   - Включает "Коллективный сбор" для iPhone
   - Копирует публичную ссылку

2. **Друг 1 (Петя):**
   - Открывает ссылку без регистрации
   - Видит iPhone 100,000₽, прогресс 0%
   - Жмёт "Скинуться" → 30,000₽
   - **Real-time**: прогресс обновился → 30%

3. **Друг 2 (Вася):**
   - Открывает ту же ссылку (параллельно!)
   - **Сразу видит**: Петя скинулся 30,000₽ (БЕЗ перезагрузки!)
   - Скидывается ещё 70,000₽
   - Прогресс → 100% ✅ "Собрано!"

4. **Маша (владелец):**
   - Смотрит дашборд
   - Видит: "🎉 iPhone — 100,000 / 100,000₽"
   - **НЕ видит**: что Петя дал 30к, Вася 70к
   - Сюрприз сохранён! 🎁

## 🎨 Дизайн

- **Не дефолтные стили**: кастомные градиенты, тени, анимации
- **Цвета**: Primary blue → purple gradient
- **Иконки**: React Icons (FaGift, FaLock, FaUsers)
- **Анимации**: Progress bar transition, модальные окна fade
- **Адаптив**: Mobile-first подход

## 📝 TODO для production

- [ ] Деплой на Vercel (frontend) + Railway (backend + DB)
- [ ] OAuth (Google, GitHub)
- [ ] Email уведомления участникам
- [ ] Отмена вкладов
- [ ] История изменений
- [ ] Экспорт в PDF

## 👨‍💻 Разработка с AI

Весь код написан с помощью Cursor AI:
- Архитектура предложена AI на основе требований
- Real-time WebSocket реализован за 5 минут
- URL парсер с поддержкой популярных магазинов
- Секретные схемы для owner/guest

---

**Сделано с ❤️ и Cursor AI**
